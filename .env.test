# Configurazione database per i test di integrazione
# Copia questo file in .env.test e modifica i valori secondo il tuo setup

# === CONFIGURAZIONE LOCALE (Per test in locale) ===
# Quando esegui i test dal tuo PC locale
TEST_DB_HOST=localhost
TEST_DB_PORT=5433
TEST_DB_USER=coworkspace_test_user
TEST_DB_PASSWORD=test_password_secure
TEST_DB_NAME=coworkspace_test_db

# === CONFIGURAZIONE DOCKER ===
# Quando usi docker-compose.test.yml (commentate per ora)
# TEST_DB_HOST=test-db
# TEST_DB_PORT=5432
# TEST_DB_USER=coworkspace_test_user
# TEST_DB_PASSWORD=test_password_secure
# TEST_DB_NAME=coworkspace_test_db

# Oppure usa una stringa di connessione completa
# TEST_DATABASE_URL=postgresql://coworkspace_test_user:test_password_secure@localhost:5433/coworkspace_test_db

# JWT per i test
JWT_SECRET=test-jwt-secret-very-long-and-secure-key-for-testing-only
JWT_EXPIRES_IN=24h

# Ambiente
NODE_ENV=test

# Debug dei test (opzionale)
TEST_VERBOSE=false

# Disabilita servizi esterni nei test
EMAIL_USER=""
EMAIL_PASS=""
FIREBASE_SERVICE_ACCOUNT_PATH=""